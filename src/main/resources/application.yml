server:
  port: 25565
spring:
  datasource:
    url: "jdbc:mysql://localhost/promotion?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Shanghai"
    driver-class-name: "com.mysql.cj.jdbc.Driver"
    username: "promotion"
    password: "promotion"
  jpa:
    database: "MySQL"
    database-platform: "org.hibernate.dialect.MySQL5Dialect"
    show-sql: true
    hibernate:
      ddl-auto: "update"
  redis:
    host: "localhost"